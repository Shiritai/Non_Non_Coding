# include <stdio.h>
int main(void){
    // int year, k;
    // scanf("%d", &year);
    // k = (!(year % 400)) ? 1 : \
    //     (!(year % 4) && year % 100) ? 1 : 0;
    printf("%s", k ? "Leap year" : "Normal year");
}